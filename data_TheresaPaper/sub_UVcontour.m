function [sgmX,sgmY] = sub_contour(IIs,JJs);
% find contour going through U,V points
% given indices of the grid cell p-points (mid points);
%
nsg=length(IIs);
for is=1:nsg
  i0=IIs(is);
  j0=JJs(is);


return
